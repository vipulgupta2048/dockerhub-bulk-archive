# Docker Hub credentials

# Required: Your Docker Hub username
DOCKER_USERNAME=your-username

# Required: Your Docker Hub password
DOCKER_PASSWORD=your-password

# Required for 2FA: Your TOTP secret key
# This is the secret key used by authenticator apps (Google Authenticator, Authy, etc.)
# This is NOT the 6-digit code, but the secret key used to generate those codes
DOCKER_TOTP_SECRET=your-totp-secret-key

# Note: Copy this file to .env and replace the values with your actual credentials
# Do not commit the .env file to version control 